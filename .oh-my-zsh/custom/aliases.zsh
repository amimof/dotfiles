# Re-attach to tmux session. Create a new session if none exists
alias s='tmux attach || tmux new'

# git repos home
alias gh="cd ~/git && pwd"

# Alias kubectl to k
alias k="kubectl"

# Alias kubectx to kx
alias kx="kubectx"